<%= simple_form_for(@sistema, remote: true) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :edifici_id %>
    <%= f.input :nom_sistema %>
    <%= f.input :pare_sistema %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
